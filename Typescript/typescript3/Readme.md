### Commands

1. Create a folder 
  cd typescript
  npm init -y
  npm install typescript
  npx tsc --init => tsconfig.json
  touch .gitignore
  mkdir src 
  comment out these 

    "rootDir": "./src",
    "outDir": "./dist",

  to check output npx tsc -b